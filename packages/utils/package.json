{
  "name": "@timeoff/utils",
  "version": "1.0.0",
  "description": "Shared utility functions for the Timeoff Management System",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "zod": "^3.22.4"
  },
  "peerDependencies": {
    "@timeoff/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
} 